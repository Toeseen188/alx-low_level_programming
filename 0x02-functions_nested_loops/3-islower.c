#include "main.h"
/**
 * _islower - returns the args as lowercase if true
 * Return: returns an interger value, true = 1
 * @c : parameter for islower
*/

int _islower(int c)
{

	return (c >= 'a' && c <= 'z');
}
