#include <stdio.h>
/**
 * main - contains all my compilable codes
 * Return:always returns 0 to mean success
 */
int main(void)
{
	char c;

	for (c = 'a' ; c <= 'z' ; c++)
	putchar(c);
	putchar('\n');
	return (0);
}
